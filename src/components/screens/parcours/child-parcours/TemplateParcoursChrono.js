function TemplateParcoursChrono(content, cardSubtitle, url, image) {

    return(
        <div>
            <p>{content}</p>
            <p>{url}</p>
            <p>{cardSubtitle}</p>

        </div>
    )
}

export default TemplateParcoursChrono;

/*<img src={image} alt={title}/>*/